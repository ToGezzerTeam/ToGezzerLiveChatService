services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: togezzer-rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: togezzer-live-chat-service
    restart: unless-stopped
    network_mode: host  # accès direct aux ports, pas de NAT
    depends_on:
      rabbitmq:
        condition: service_healthy
    env_file:
      - .env
    environment:
      PORT: 3000
      RABBITMQ_HOST: localhost  # host car network_mode: host
      MEDIASOUP_ANNOUNCED_IP: ${MEDIASOUP_ANNOUNCED_IP:-127.0.0.1}
      MEDIASOUP_MIN_PORT: ${MEDIASOUP_MIN_PORT:-40000}
      MEDIASOUP_MAX_PORT: ${MEDIASOUP_MAX_PORT:-57000}